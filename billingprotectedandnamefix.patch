diff --git a/convex/auth.ts b/convex/auth.ts
index 111dcf1..5900c8d 100644
--- a/convex/auth.ts
+++ b/convex/auth.ts
@@ -11,7 +11,19 @@ import {Password} from '@convex-dev/auth/providers/Password'
 const FREE_SIGNUP_CREDITS = 10
 
 export const { auth, signIn, signOut, store, isAuthenticated } = convexAuth({
-  providers: [Google, Password],
+  providers: [
+    Google,
+    // Default Password provider sirf email save karta hai. Signup par bheja gaya
+    // `name` bhi user document me save karne ke liye custom profile() chahiye.
+    Password({
+      profile(params) {
+        return {
+          email: params.email as string,
+          name: (params.name as string | undefined) ?? undefined,
+        }
+      },
+    }),
+  ],
   callbacks: {
     async afterUserCreatedOrUpdated(ctx, args) {
       // Sirf naye users ke liye — update par kuch mat karo
diff --git a/src/lib/permissions.ts b/src/lib/permissions.ts
index f2bef88..e62037f 100644
--- a/src/lib/permissions.ts
+++ b/src/lib/permissions.ts
@@ -1,5 +1,5 @@
 export const isPublicRoutes = ['/auth(.*)', '/']
-export const isProtectedRoutes = ['/dashboard(.*)']
+export const isProtectedRoutes = ['/dashboard(.*)', '/billing(.*)']
 
 export const isByPassRoutes = [
     '/api/polar/webhook',
diff --git a/src/types/user.ts b/src/types/user.ts
index 49052ff..0f61f77 100644
--- a/src/types/user.ts
+++ b/src/types/user.ts
@@ -29,7 +29,12 @@ export const normalizeProfile = (raw: ConvexUserRaw | null): Profile | null => {
             .join(' ')
     }
 
-    const name = combinedSlug(raw.name!) || extractNameFromEmail(raw.email)
+    // Naam mojood ho to usse slug banao, warna email se — taake khaali naam par
+    // "untitled" ke bajaye sahi slug (jaise email username) mile.
+    const rawName = (raw.name ?? '').trim()
+    const name = rawName
+        ? combinedSlug(rawName)
+        : combinedSlug(extractNameFromEmail(raw.email))
 
     return {
         id: raw._id,
