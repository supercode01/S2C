diff --git a/src/components/canvas/shapes/generatedui/index.tsx b/src/components/canvas/shapes/generatedui/index.tsx
index e838892..6d86fb6 100644
--- a/src/components/canvas/shapes/generatedui/index.tsx
+++ b/src/components/canvas/shapes/generatedui/index.tsx
@@ -50,6 +50,7 @@ const GeneratedUI = ({ shape, toggleChat, generateWorkflow, exportDesign }: Prop
     const { containerRef } = useUpdateContainer(shape)
     const dispatch = useAppDispatch()
     const scale = useAppSelector((s) => s.viewport.scale) || 1
+    const currentTool = useAppSelector((s) => s.shapes.present?.tool ?? 'select')
 
     const [overlay, setOverlay] = useState<Overlay | null>(null)
     const [selectedEl, setSelectedEl] = useState<HTMLElement | null>(null)
@@ -238,9 +239,15 @@ const GeneratedUI = ({ shape, toggleChat, generateWorkflow, exportDesign }: Prop
         el.style.top = y + 'px'
         el.style.width = w + 'px'
         el.style.height = h + 'px'
-        // Preserve original paint order so reparenting (to end of scope) does not
-        // bring this element in front of elements that were above it.
-        el.style.zIndex = el.dataset.guiZ || '1'
+        // Stacking by AREA: small elements (text / buttons / images) go in FRONT
+        // so they stay visible when dragged; large containers / backgrounds go
+        // BEHIND (negative z, even behind the in-flow content) so dragging them
+        // doesn't cover everything above. Front/Back buttons can override this.
+        const designArea = (scope.offsetWidth || 1) * (scope.offsetHeight || 1)
+        const elArea = Math.max(1, w * h)
+        const z = elArea > designArea * 0.25 ? -1 : Math.max(1, Math.round(100000 / elArea))
+        el.style.zIndex = String(z)
+        el.dataset.guiZ = String(z)
         el.dataset.guiLifted = '1'
         scope.appendChild(el) // reparent within the scoped root so its CSS still applies
         computeOverlay(el)
@@ -249,6 +256,18 @@ const GeneratedUI = ({ shape, toggleChat, generateWorkflow, exportDesign }: Prop
     // ---- element interactions (click = select, drag = move) ----
     const handleContentPointerDown = (e: React.PointerEvent) => {
         const target = e.target as HTMLElement
+        // Eraser tool active → clicking an element deletes it (instead of select).
+        if (currentTool === 'eraser') {
+            if (target && target !== contentRef.current) {
+                e.preventDefault()
+                e.stopPropagation()
+                removePlaceholder(target)
+                target.remove()
+                clearSelection()
+                persist()
+            }
+            return
+        }
         if (target === contentRef.current) {
             clearSelection()
             return
@@ -340,9 +359,6 @@ const GeneratedUI = ({ shape, toggleChat, generateWorkflow, exportDesign }: Prop
                 d.startX = ev.clientX
                 d.startY = ev.clientY
             }
-            // Bring the element being dragged/resized to the FRONT so it stays
-            // visible while you move it (it was dropping behind other elements).
-            d.el.style.zIndex = '2147483646'
         }
         const dx = (ev.clientX - d.startX) / scale
         const dy = (ev.clientY - d.startY) / scale
@@ -370,9 +386,6 @@ const GeneratedUI = ({ shape, toggleChat, generateWorkflow, exportDesign }: Prop
         const d = dragRef.current
         dragRef.current = null
         if (d?.moved) {
-            // Restore the element's natural stacking (by original order) so a
-            // dragged background does not stay on top of the text over it.
-            d.el.style.zIndex = d.el.dataset.guiZ || '1'
             computeOverlay(d.el)
             persist()
             // After actually dragging an element, deselect it on release.
@@ -531,8 +544,8 @@ const GeneratedUI = ({ shape, toggleChat, generateWorkflow, exportDesign }: Prop
     const sendToBack = () => {
         if (!selectedEl) return
         liftToAbsolute(selectedEl)
-        selectedEl.dataset.guiZ = '0'
-        selectedEl.style.zIndex = '0'
+        selectedEl.dataset.guiZ = '-1'
+        selectedEl.style.zIndex = '-1'
         refresh()
         schedulePersist()
     }
@@ -770,7 +783,7 @@ const GeneratedUI = ({ shape, toggleChat, generateWorkflow, exportDesign }: Prop
                             {overlay && (
                                 <div
                                     className="absolute"
-                                    style={{ left: overlay.left, top: overlay.top, width: overlay.width, height: overlay.height, outline: '1.5px solid #6366f1', pointerEvents: 'none', zIndex: 40 }}
+                                    style={{ left: overlay.left, top: overlay.top, width: overlay.width, height: overlay.height, outline: `${2.5 / scale}px solid #6366f1`, outlineOffset: `${1 / scale}px`, pointerEvents: 'none', zIndex: 40 }}
                                 >
                                     {/* move grip — drags the selected element (works for containers too) */}
                                     <div
@@ -807,7 +820,7 @@ const GeneratedUI = ({ shape, toggleChat, generateWorkflow, exportDesign }: Prop
                 </div>
             </div>
 
-            <style>{`.gui-editing *:hover { outline: 1px dashed rgba(99,102,241,0.5); outline-offset: 2px; cursor: pointer; }`}</style>
+            <style>{`.gui-editing *:hover { outline: 1px dashed rgba(99,102,241,0.5); outline-offset: 2px; cursor: ${currentTool === 'eraser' ? 'crosshair' : 'pointer'}; }`}</style>
 
             {/* Whole-block move handle */}
             <div
@@ -823,7 +836,7 @@ const GeneratedUI = ({ shape, toggleChat, generateWorkflow, exportDesign }: Prop
             {blockSelected && (
                 <div
                     className="absolute inset-0 pointer-events-none"
-                    style={{ outline: '1.5px solid #6366f1', zIndex: 45 }}
+                    style={{ outline: `${2.5 / scale}px solid #6366f1`, zIndex: 45 }}
                 >
                     {(['nw', 'ne', 'sw', 'se'] as DragMode[]).map((corner) => (
                         <div
